# Springboot Docker REST API

#### Instructions
1. Install [Docker](https://www.docker.com/) if it's already not installed.
2. Build the docker image `docker build -t places`.
3. Run the docker container `docker run -d -p 8080:8080 places`.